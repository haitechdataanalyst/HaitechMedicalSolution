import { Headset, LifeBuoy, FileCheck, Mail, Phone, MessageCircle, Info, PlaySquare, HelpCircle, FileText, GitCompare, Shield, BadgeCheck, RotateCcw, type LucideIcon } from "lucide-react";

export const iconMap: Record<string, LucideIcon> = {
    Headset,
    LifeBuoy,
    FileCheck,
    Mail,
    Phone,
    MessageCircle,
    Info,
    PlaySquare,
    HelpCircle,
    FileText,
    GitCompare,
    Shield,
    BadgeCheck,
    RotateCcw,
};

export type { LucideIcon };
