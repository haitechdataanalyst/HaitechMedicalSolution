// List routes that MUST provide X-Api-Key.
// Path supports static segments and parameter tokens such as /foo/:id.
export const routesWithApiKey = [
	// Example:
	// { method: 'POST', path: '/api/v1/webhooks/razorpay' },
];
