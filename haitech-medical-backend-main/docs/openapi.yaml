openapi: 3.1.0
info:
  title: Haitech Medical Backend API
  description: >
    Production-ready REST API for the Haitech Medical e-commerce platform.
    Handles authentication, user management, order processing, and Razorpay payments.
  version: 1.1.0
  contact:
    name: Haitech Medical Engineering
    email: tech@haitech-group.com
  license:
    name: ISC

servers:
  - url: http://localhost:{port}/api/{version}
    description: Local development
    variables:
      port:
        default: "5000"
      version:
        default: v1
  - url: https://haitechmedical.com.au/api/v1
    description: Production

tags:
  - name: Auth
    description: Authentication, token management, password and email verification
  - name: Users
    description: User profile and address management
  - name: Orders
    description: Order placement and lifecycle management
  - name: Payments
    description: Razorpay payment creation and verification
  - name: Health
    description: Service health checks and readiness probes

# ── Security Schemes ──────────────────────────────────────────────────────────
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: "JWT access token obtained via POST /auth/login. Send as Authorization: Bearer <token>."
    CsrfToken:
      type: apiKey
      in: header
      name: X-CSRF-Token
      description: CSRF token for cookie-based auth. Obtain via GET /auth/csrf-token.

  # ── Reusable Schemas ────────────────────────────────────────────────────────
  schemas:

    SuccessResponse:
      type: object
      properties:
        timestamp:   { type: string, format: date-time }
        statusCode:  { type: integer, example: 200 }
        status:      { type: integer, example: 200 }
        success:     { type: boolean, example: true }
        message:     { type: string, example: Success }
        data:        { type: object, nullable: true }

    ErrorResponse:
      type: object
      properties:
        timestamp:   { type: string, format: date-time }
        statusCode:  { type: integer, example: 400 }
        status:      { type: integer, example: 400 }
        success:     { type: boolean, example: false }
        message:     { type: string, example: Validation error }
        error:       { type: object, nullable: true }

    PaginationMeta:
      type: object
      properties:
        page:        { type: integer, example: 1 }
        limit:       { type: integer, example: 10 }
        total:       { type: integer, example: 42 }
        totalPages:  { type: integer, example: 5 }

    User:
      type: object
      properties:
        id:           { type: string, format: uuid }
        firstName:    { type: string, example: Ranvijay }
        lastName:     { type: string, example: Singh }
        username:     { type: string, example: ranvijay }
        email:        { type: string, format: email }
        phone:        { type: string, example: "9876543210", nullable: true }
        roles:        { type: array, items: { type: string }, example: [user] }
        role:         { type: string, example: user, nullable: true }
        emailVerified: { type: boolean, example: false }
        phoneVerified: { type: boolean, example: false }
        authProvider: { type: string, example: local }
        active:       { type: boolean, example: true }
        createdAt:    { type: string, format: date-time }
        modifiedAt:   { type: string, format: date-time }

    AuthSession:
      type: object
      properties:
        user:         { $ref: "#/components/schemas/User" }
        accessToken:  { type: string, description: Short-lived JWT access token }
        refreshToken: { type: string, description: Long-lived JWT refresh token (also set as httpOnly cookie) }

    Address:
      type: object
      properties:
        id:           { type: string, format: uuid }
        userId:       { type: string, format: uuid }
        fullName:     { type: string, example: Dr. Priya Mehta }
        phone:        { type: string, example: "9876543210" }
        addressLine1: { type: string, example: 101 MG Road }
        addressLine2: { type: string, nullable: true }
        landmark:     { type: string, nullable: true }
        city:         { type: string, example: Mumbai }
        state:        { type: string, example: Maharashtra }
        postalCode:   { type: string, example: "400001" }
        country:      { type: string, example: India }
        addressType:  { type: string, enum: [home, work, other] }
        isDefault:    { type: boolean }

    OrderItem:
      type: object
      properties:
        id:           { type: string, format: uuid }
        orderId:      { type: string, format: uuid }
        productId:    { type: string, example: admetec-ergo-2.5 }
        productName:  { type: string, example: "Admetec Ergo 2.5x Loupe" }
        productBrand: { type: string, nullable: true }
        productSku:   { type: string, nullable: true }
        quantity:     { type: integer, minimum: 1 }
        unitPrice:    { type: integer, description: Unit price in paise (1 INR = 100 paise) }
        totalPrice:   { type: integer, description: Total price in paise }

    Order:
      type: object
      properties:
        id:              { type: string, format: uuid }
        userId:          { type: string, format: uuid }
        status:          { type: string, enum: [pending, confirmed, shipped, delivered, cancelled] }
        paymentStatus:   { type: string, enum: [unpaid, paid, refunded] }
        razorpayOrderId: { type: string, nullable: true }
        subtotal:        { type: integer, description: In paise }
        tax:             { type: integer, description: In paise }
        shippingFee:     { type: integer, description: In paise }
        total:           { type: integer, description: In paise }
        currency:        { type: string, example: INR }
        notes:           { type: string, nullable: true }
        items:           { type: array, items: { $ref: "#/components/schemas/OrderItem" } }
        createdAt:       { type: string, format: date-time }

    HealthStatus:
      type: object
      properties:
        status:      { type: string, enum: [healthy, degraded] }
        version:     { type: string, example: v1 }
        nodeVersion: { type: string, example: v22.0.0 }
        uptime:      { type: number }
        memory:      { type: object }
        dependencies:
          type: object
          properties:
            database: { type: object, properties: { status: { type: string } } }
            redis:    { type: object, properties: { status: { type: string }, circuitBreaker: { type: string } } }

# ── Paths ─────────────────────────────────────────────────────────────────────
paths:

  # ── Auth ────────────────────────────────────────────────────────────────────

  /auth/login:
    post:
      tags: [Auth]
      summary: Login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:    { type: string, format: email }
                password: { type: string, format: password }
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AuthSession" }
        "401": { description: Invalid credentials, content: { application/json: { schema: { $ref: "#/components/schemas/ErrorResponse" } } } }

  /auth/register:
    post:
      tags: [Auth]
      summary: Register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [firstName, lastName, username, email, phone, password]
              properties:
                firstName: { type: string }
                lastName:  { type: string }
                username:  { type: string, minLength: 3, maxLength: 50 }
                email:     { type: string, format: email }
                phone:     { type: string, example: "9876543210" }
                password:  { type: string, format: password, description: "Min 8 chars, 1 upper, 1 lower, 1 number, 1 special" }
      responses:
        "201": { description: Registration successful, content: { application/json: { schema: { allOf: [ { $ref: "#/components/schemas/SuccessResponse" }, { type: object, properties: { data: { $ref: "#/components/schemas/AuthSession" } } } ] } } } }
        "409": { description: Email or username already taken }
        "422": { description: Validation error }

  /auth/google/signin:
    post:
      tags: [Auth]
      summary: Google sign-in
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [googleSub, email, firstName, lastName]
              properties:
                googleSub:  { type: string }
                email:      { type: string, format: email }
                firstName:  { type: string }
                lastName:   { type: string }
                username:   { type: string }
                phone:      { type: string }
      responses:
        "200": { description: Google sign-in successful }

  /auth/logout:
    post:
      tags: [Auth]
      summary: Logout
      description: Revokes the refresh token. Cookie is cleared automatically.
      security:
        - BearerAuth: []
        - CsrfToken: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                refreshToken: { type: string }
      responses:
        "200": { description: Logged out successfully }

  /auth/refresh-token:
    post:
      tags: [Auth]
      summary: Refresh access token
      description: Issues a new access + refresh token pair. Old refresh token is revoked (rotation).
      security:
        - CsrfToken: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                refreshToken: { type: string }
      responses:
        "200":
          description: Token refreshed
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          accessToken:  { type: string }
                          refreshToken: { type: string }
        "401": { description: Invalid or expired refresh token }

  /auth/forgot-password:
    post:
      tags: [Auth]
      summary: Request password reset
      description: Sends reset link to the email if registered. Always returns 200 (no email enumeration).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email: { type: string, format: email }
      responses:
        "200": { description: Reset email sent (if registered) }

  /auth/reset-password:
    post:
      tags: [Auth]
      summary: Reset password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, password]
              properties:
                token:    { type: string }
                password: { type: string, format: password }
      responses:
        "200": { description: Password reset successful }
        "400": { description: Invalid or expired token }

  /auth/verify-email:
    get:
      tags: [Auth]
      summary: Verify email address
      parameters:
        - in: query
          name: token
          required: true
          schema: { type: string }
      responses:
        "200": { description: Email verified }
        "400": { description: Invalid or expired token }

  /auth/resend-verification-email:
    post:
      tags: [Auth]
      summary: Resend verification email
      security:
        - BearerAuth: []
      responses:
        "200": { description: Verification email sent }
        "409": { description: Email already verified }

  /auth/csrf-token:
    get:
      tags: [Auth]
      summary: Get CSRF token
      responses:
        "200": { description: CSRF cookie set }

  # ── Users ────────────────────────────────────────────────────────────────────

  /users/me:
    get:
      tags: [Users]
      summary: Get my profile
      security:
        - BearerAuth: []
      responses:
        "200":
          description: User profile
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          user: { $ref: "#/components/schemas/User" }
        "401": { description: Unauthorized }
    put:
      tags: [Users]
      summary: Update profile
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName: { type: string }
                lastName:  { type: string }
                username:  { type: string, minLength: 3 }
                phone:     { type: string, nullable: true }
      responses:
        "200": { description: Profile updated }
        "409": { description: Username taken }

  /users/me/password:
    put:
      tags: [Users]
      summary: Change password
      description: Revokes all active tokens after a successful change, forcing re-login on all devices.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [currentPassword, newPassword]
              properties:
                currentPassword: { type: string, format: password }
                newPassword:     { type: string, format: password }
      responses:
        "200": { description: Password changed }
        "401": { description: Current password incorrect }

  /users/me/addresses:
    get:
      tags: [Users]
      summary: List addresses
      security:
        - BearerAuth: []
      responses:
        "200":
          description: Address list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          addresses: { type: array, items: { $ref: "#/components/schemas/Address" } }
    post:
      tags: [Users]
      summary: Add address
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [fullName, phone, addressLine1, city, state, postalCode]
              properties:
                fullName:     { type: string }
                phone:        { type: string }
                addressLine1: { type: string }
                addressLine2: { type: string, nullable: true }
                landmark:     { type: string, nullable: true }
                city:         { type: string }
                state:        { type: string }
                postalCode:   { type: string }
                country:      { type: string, default: India }
                addressType:  { type: string, enum: [home, work, other], default: home }
                isDefault:    { type: boolean, default: false }
      responses:
        "201": { description: Address created }

  /users/me/addresses/{id}:
    put:
      tags: [Users]
      summary: Update address
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200": { description: Address updated }
        "404": { description: Address not found }
    delete:
      tags: [Users]
      summary: Delete address
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200": { description: Address removed }

  /users/me/addresses/{id}/default:
    put:
      tags: [Users]
      summary: Set default address
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200": { description: Default address updated }

  # ── Orders ────────────────────────────────────────────────────────────────────

  /orders:
    post:
      tags: [Orders]
      summary: Place order
      description: Creates an order from cart items. Prices must be in paise.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [items]
              properties:
                items:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required: [productId, productName, quantity, unitPrice, totalPrice]
                    properties:
                      productId:    { type: string }
                      productName:  { type: string }
                      productBrand: { type: string, nullable: true }
                      productSku:   { type: string, nullable: true }
                      quantity:     { type: integer, minimum: 1 }
                      unitPrice:    { type: integer, description: In paise }
                      totalPrice:   { type: integer, description: In paise }
                shippingAddressId: { type: string, format: uuid, nullable: true }
                notes:             { type: string, maxLength: 500, nullable: true }
      responses:
        "201":
          description: Order placed
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          order: { $ref: "#/components/schemas/Order" }
    get:
      tags: [Orders]
      summary: List my orders
      security:
        - BearerAuth: []
      parameters:
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: limit
          schema: { type: integer, default: 10, maximum: 50 }
      responses:
        "200":
          description: Order list with pagination
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          orders: { type: array, items: { $ref: "#/components/schemas/Order" } }
                      meta: { $ref: "#/components/schemas/PaginationMeta" }

  /orders/{id}:
    get:
      tags: [Orders]
      summary: Get order detail
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200": { description: Order detail with items }
        "404": { description: Order not found }

  /orders/{id}/cancel:
    put:
      tags: [Orders]
      summary: Cancel order
      description: Only pending or confirmed orders can be cancelled.
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200": { description: Order cancelled }
        "400": { description: Order cannot be cancelled }
        "404": { description: Order not found }

  /orders/admin/all:
    get:
      tags: [Orders]
      summary: "[Admin] List all orders"
      security:
        - BearerAuth: []
      parameters:
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: limit
          schema: { type: integer, default: 20, maximum: 100 }
        - in: query
          name: status
          schema: { type: string, enum: [pending, confirmed, shipped, delivered, cancelled] }
      responses:
        "200": { description: All orders paginated }
        "403": { description: Admin access required }

  /orders/admin/{id}/status:
    put:
      tags: [Orders]
      summary: "[Admin] Update order status"
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [pending, confirmed, shipped, delivered, cancelled] }
      responses:
        "200": { description: Status updated }
        "403": { description: Admin access required }
        "404": { description: Order not found }

  # ── Payments ──────────────────────────────────────────────────────────────────

  /payments/orders/{orderId}:
    post:
      tags: [Payments]
      summary: Create Razorpay order
      description: >
        Creates a Razorpay order for the given internal order ID.
        Returns the Razorpay order ID and key ID needed to initialise the Razorpay checkout SDK.
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: orderId
          required: true
          schema: { type: string, format: uuid }
      responses:
        "201":
          description: Razorpay order created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          razorpayOrderId: { type: string }
                          amount:          { type: integer, description: In paise }
                          currency:        { type: string, example: INR }
                          keyId:           { type: string }
        "400": { description: Order already paid or not found }

  /payments/verify:
    post:
      tags: [Payments]
      summary: Verify payment signature
      description: >
        Validates the HMAC-SHA256 signature from Razorpay.
        On success, marks the order as paid and status as confirmed.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [orderId, razorpayOrderId, razorpayPaymentId, razorpaySignature]
              properties:
                orderId:             { type: string, format: uuid }
                razorpayOrderId:     { type: string }
                razorpayPaymentId:   { type: string }
                razorpaySignature:   { type: string }
      responses:
        "200":
          description: Payment verified, order confirmed
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          order: { $ref: "#/components/schemas/Order" }
        "400": { description: Invalid payment signature }

  # ── Health ───────────────────────────────────────────────────────────────────

  /health:
    get:
      tags: [Health]
      summary: Deep health check
      responses:
        "200": { description: Healthy }
        "503": { description: Degraded }

  /health/live:
    servers:
      - url: http://localhost:{port}
        variables:
          port:
            default: "5000"
    get:
      tags: [Health]
      summary: Liveness probe
      responses:
        "200": { description: Process alive }

  /health/ready:
    servers:
      - url: http://localhost:{port}
        variables:
          port:
            default: "5000"
    get:
      tags: [Health]
      summary: Readiness probe
      responses:
        "200": { description: Ready }
        "503": { description: Not ready }

  /api/versions:
    servers:
      - url: http://localhost:{port}
        variables:
          port:
            default: "5000"
    get:
      tags: [Health]
      summary: Supported API versions
      responses:
        "200": { description: Version info }
